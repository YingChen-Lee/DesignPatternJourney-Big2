#include "player.h"

/*
void Player::GainPoint() {
    point_++;
}

void Player::ResetData() {
    point_ = 0;
    hand_->Reset();
    name_ = "";
}

void Player::AddCardToHand(std::shared_ptr<const Card> card) {
    hand_->AddCard(card);
}

int Player::GetHandSize() const {
    return hand_->get_size();
}

std::shared_ptr<const Card> Player::ShowCard(std::shared_ptr<ShowCardContext> context) {
    hand_->SortAndPrintHand();
    return strategy_->ShowCard(context, hand_);
}

*/